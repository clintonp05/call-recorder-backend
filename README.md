# call-recorder

**mac**
python -m venv env
source env/bin/activate

**windows**
python -m venv env
env/bin/activate

pip install openai
pip install python-docx


to run the server

python main.py

python3 main.py
